# one of [trace, debug, info, warn, error]
log_level = "info"

[web]
host = "0.0.0.0"
port = 4000
allowed_origins = ["", "http://localhost:3000"]

[spotify]
client_id = "change_me"
client_secret = "change_me"
redirect_uri = "http://localhost:4000/api/redirect"
